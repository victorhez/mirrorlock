'use client'; import Link from 'next/link'; import {usePathname} from 'next/navigation'; import {BrainCircuit,BookOpenCheck,ChartNoAxesCombined,History,ShieldCheck} from 'lucide-react';
const links=[['/challenge','Challenge'],['/trades','Paper trades'],['/insights','Mirror insights']];
export function Nav(){const p=usePathname();return <header className="sticky top-0 z-50 border-b border-white/6 bg-[#07090d]/75 backdrop-blur-xl"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5"><Link href="/challenge" className="flex items-center gap-2.5"><span className="grid size-9 place-items-center rounded-xl bg-white text-black"><ShieldCheck size={19}/></span><span className="font-semibold tracking-tight">MirrorLock</span></Link><nav className="hidden gap-1 md:flex">{links.map(([href,label])=><Link key={href} href={href} className={`rounded-lg px-3 py-2 text-sm ${p.startsWith(href)?'bg-white/9 text-white':'text-white/55 hover:text-white'}`}>{label}</Link>)}</nav><div className="flex items-center gap-2 text-xs text-white/45"><span className="size-2 rounded-full bg-emerald-400"/> Paper mode</div></div></header>}
