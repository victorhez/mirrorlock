export default async function Share({params}:{params:Promise<{id:string}>}){const {id}=await params;return <main className="grid-bg min-h-[calc(100vh-4rem)]"><div className="mx-auto max-w-2xl px-5 py-20 text-center"><div className="mx-auto grid size-14 place-items-center rounded-2xl bg-white text-black">🪞</div><div className="mt-7 text-xs uppercase tracking-[.2em] text-white/35">MirrorLock challenge</div><h1 className="mt-4 text-5xl font-semibold tracking-tight">Challenge ID {id.slice(0,8)}</h1><p className="mt-5 text-white/45">A public share card is ready for this decision. Connect the persistence layer to render the stored verdict, confidence gap and Mirror objection.</p><div className="glass mt-8 rounded-3xl p-6 text-left"><div className="text-sm font-semibold">Why this matters</div><p className="mt-3 text-sm leading-6 text-white/45">MirrorLock turns confidence into something challengeable: your thesis is tested, risk rules are enforced, and the system records what would change its mind.</p></div></div></main>}
